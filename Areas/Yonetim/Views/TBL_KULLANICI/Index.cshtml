@model IEnumerable<YonetimPanel48.Models.TBL_KULLANICI>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.K_ADI)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.K_PAROLA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.K_ADI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.K_PAROLA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.K_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.K_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.K_ID })
        </td>
    </tr>
}

</table>
